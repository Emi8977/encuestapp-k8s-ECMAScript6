apiVersion: apps/v1
kind: Deployment
metadata:
  name: encuestapp
spec:
  template:
    spec:
      containers:
        - name: encuestapp
          image: emi8977/encuestapp-mern-ECMAScript6:REPLACEME

